
// This is kind of a test. I'm not exactly sure how to implement SDFs

import {Element} from "../core/element"

class TextElement extends Element {
  constructor (params={}) {
    super(params)
  }

  render (renderer) {
    return (renderer) => {

    }
  }
}
